spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        formate-sql: true
    database: postgresql


application:
  security:
    jwt:
      secret-key: 94AL95EE88NI58A50
      expiration: 8281449468